@top Nix { expression* }

expression { Add | Int }

@precedence {
  Add @left
}

@skip { whitespace | LineComment }

Add { expression !Add "+" expression }

@tokens {
  Int {
    std.digit+
  }

  whitespace { std.whitespace+ }

  LineComment { "#" ![\n]* }
}
